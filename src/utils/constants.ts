import { AxisMetrics } from "../types/config/axis.type";

export const MIN_THUMB_SIZE = 24;
export const DEFAULT_TRACK_THICKNESS = 8;
export const MAX_THUMB_RATIO = 0.8;

export const EMPTY_AXIS_METRICS: AxisMetrics = {
    scrollSize: 0,
    clientSize: 0,
    scrollPos: 0,
    canScroll: false,
};
